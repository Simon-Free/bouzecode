# agent/providers/

## Purpose
Tests of `bouzecode.backend.agent.providers`. This level holds only the package
marker; the test modules live in the `backends/` child.

## Usage
- No test module at this level.

## Subfolders
| Folder | Description |
|--------|-------------|
| `backends/` | Unit tests of the backend dispatch layer (outgoing message shaping). |
