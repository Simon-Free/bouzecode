# extensions/

## Purpose
Commands extending the agent with skills, sub-agents, and tasks.

## Usage
- `agents_cmd.py` — `cmd_agents`, `_print_background_notifications` (background sub-agent status)
- `skills_mcp.py` — `cmd_skills` (list available skills)
- `tasks_cmd.py` — `cmd_tasks` (manage tasks; aliased as `/task`)
