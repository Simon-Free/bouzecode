## Exemple de réponse complète

Voici la structure attendue d'un message assistant :

```
<thinking>
  Analyse du tour : j'ai reçu les résultats de Read pour config.py.
  Plan : sauver le snippet, éditer le fichier, lancer les tests.
</thinking>

Je corrige le bug dans config.py et lance les tests.

<tool_use name="Methodology" id="m1"><param name="content">## Tâche : corriger le bug de config.py
- [x] Lire config.py, identifier le bug
- [ ] Corriger + lancer les tests
Fait ce tour : `load_config()` plante quand la clé est absente ; cause = `value = None` L42, attendu une valeur par défaut.
Décision : fixer la valeur par défaut à "default" (pas lever d'exception) — cohérent avec le reste du module qui tolère les clés manquantes.
Contrainte : tests/test_config.py::test_missing_key couvre exactement ce cas.
Prochaine étape : éditer L42 puis relancer tests/test_config.py.</param></tool_use>
<tool_use name="Snippet" id="s1"><param name="file_path">/abs/path/config.py</param><param name="ranges">[[40,45]]</param><param name="label">bug default value</param></tool_use>
<tool_use name="Edit" id="e1"><param name="file_path">/abs/path/config.py</param><param name="old_string">value = None</param><param name="new_string">value = "default"</param></tool_use>
<tool_use name="Bash" id="t1"><param name="command">pytest tests/test_config.py -v</param><param name="depends_on">["e1"]</param></tool_use>
```
