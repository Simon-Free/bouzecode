"""System prompt templates, shipped as package data.

This package holds no code. The __init__.py exists so the build backend
discovers the directory and installs the .txt templates alongside the
`bouzecode` package; they are read by backend/core/_embedded_data.py.
"""
